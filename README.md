# 📉 Bankruptcy Prediction System

### End-to-End ML система прогнозирования вероятности банкротства компаний

Проект представляет собой **production-ориентированную ML-систему** для оценки вероятности банкротства компаний (Probability of Default, PD) на основе финансовых показателей.

Реализован **полный цикл ML-разработки**:
сбор и анализ данных → feature engineering → отбор и тюнинг модели → обучение и калибровка → интерпретация → тестирование → REST API для инференса.

---

## 🧩 Архитектура проекта (ключевые компоненты)

```text
Bankruptcy_Prediction_System/
├── api/
│   └── main.py
├── src/
│   ├── data_preprocessing.py
│   ├── train_model.py
│   ├── predict_model.py
│   ├── hyperparameters_tuning.py
│   ├── evaluating.py
│   └── constants.py
├── notebooks/
│   ├── EDA.ipynb
│   ├── feature_engineering.ipynb
│   ├── model_selection.ipynb
│   ├── hyperparameter_tuning.ipynb
│   └── final_model.ipynb
├── models/
│   ├── calibrated_model.pkl
│   └── preprocessor.pkl
├── tests/
│   ├── test_preprocessor.py
│   ├── test_trainer.py
│   ├── test_predictor.py
│   └── test_full_pipeline.py
├── config/
│   └── config.py
├── requirements.txt
└── README.md
```
---

## 🎯 Цель проекта

Построить **интерпретируемую и откалиброванную модель риск-скоринга**, пригодную для:

* оценки вероятности банкротства компаний
* финансового анализа и риск-менеджмента
* использования в production-сервисах через API

---

## 📊 Данные

* 📦 **≈700 000 компаний**
* 🧮 **110 исходных финансовых признаков**
* ⚠️ **Сильный дисбаланс классов**:
  доля банкротств ≈ **0.4% (≈1 / 250)**

### Выполнено:

* сбор и структурирование данных
* очистка и контроль качества
* анализ пропусков и выбросов
* проверка потенциальных утечек данных

---

## 🔍 Exploratory Data Analysis (EDA)

Проведён **глубокий EDA**, зафиксированный в исследовательских ноутбуках:

* анализ распределений финансовых показателей
* сравнение банкротов и небанкротов
* корреляционный анализ и мультиколлинеарность
* выявление неинформативных и нестабильных признаков
* анализ влияния дисбаланса на метрики

**Файл:** `notebooks/EDA.ipynb`

EDA использовался как основа для feature engineering и выбора модели.

---

## ⚙️ Feature Engineering

Реализован кастомный препроцессинг финансовых данных:

* генерация доменных финансовых коэффициентов
* абсолютные и относительные дельты показателей
* нормализованные отношения признаков
* обработка выбросов
* удаление признаков с нулевой дисперсией

📈 Результат:

* **110 → 235 признаков**
* стабильный и воспроизводимый feature-space

Препроцессор **stateful**:
обучается один раз и далее используется неизменным при инференсе.

**Файл:** `notebooks/feature_engineering.ipynb`
**Реализация:** `src/data_preprocessing.py`

---

## 🧠 Отбор модели и тюнинг

### Выбор модели

Проведено сравнение нескольких моделей для табличных данных с учётом сильного дисбаланса классов.

**Файл:** `notebooks/model_selection.ipynb`

По результатам выбрана модель:

* **CatBoostClassifier**
* устойчива к выбросам и нелинейностям
* хорошо работает при редких событиях

---

### Подбор гиперпараметров

* Использован **Optuna**
* оптимизация с учётом дисбаланса классов
* контроль переобучения и устойчивости метрик

**Файл:** `notebooks/hyperparameter_tuning.ipynb`
**Реализация:** `src/hyperparameters_tuning.py`

---

## 🏋️ Финальное обучение модели

В ноутбуке `final_model.ipynb` выполнено:

* обучение финальной версии модели
* оценка качества на валидации
* достижение **weighted accuracy ≈ 0.87**
* сохранение обученной модели и препроцессора

**Файл:** `notebooks/final_model.ipynb`
**Production-реализация:** `src/train_model.py`

---

## 🎯 Калибровка вероятностей

Так как задача — **оценка риска**, а не только бинарная классификация, выполнена калибровка вероятностей:

* **Isotonic Regression**
* отдельный validation-набор
* улучшено соответствие предсказанных вероятностей реальному риску

Калибровка критична для задач rare events (банкротства, дефолты).

**Реализация:** `src/train_model.py`

---

## 🔎 Интерпретируемость модели

Добавлена интерпретация через **SHAP**:

* глобальная важность признаков
* вклад отдельных признаков в предсказания
* объяснение решений модели

**Пример визуализации:** `src/waterfall_interpretation.png`
<img width="800" height="650" alt="image" src="https://github.com/user-attachments/assets/713ead89-1b27-407d-b83e-827d9218ea40" />


---

## 🧪 Тестирование

Пайплайн покрыт unit-тестами с использованием **PyTest**:

* проверка препроцессинга и валидации входных данных
* тесты обучения модели
* тесты инференса (Predictor)
* интеграционный тест полного ML-пайплайна

Запуск тестов:

```bash
pytest
```

---

## 🌐 API (FastAPI)

Реализован REST API для инференса обученной модели прогнозирования банкротства.

### Запуск сервиса

```bash
uvicorn api.main:app --reload
```

Swagger UI доступен по адресу:
[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

### Endpoint `/predict_csv`

* принимает CSV-файл с финансовыми признаками
* проверяет наличие всех обязательных колонок
* применяет сохранённый препроцессор
* возвращает **откалиброванные вероятности банкротства**

Пример ответа:

```json
{
  "n_samples": 100,
  "predictions": [0.002, 0.015, 0.41]
}
```
---
Распределение риска на тестовых данных:
<img width="3569" height="1769" alt="image" src="https://github.com/user-attachments/assets/34c4ad6a-2b14-4269-bbec-de082221309f" />


---

## 🧰 Используемые технологии

* Python
* Pandas, NumPy
* Scikit-learn
* CatBoost
* Optuna
* SHAP
* FastAPI
* PyTest
* Docker

---

## 👤 Автор

**Апухтин Владислав**
ML Engineer / Data Scientist

Проект выполнен как **production-ориентированный pet-project**, демонстрирующий полный цикл разработки ML-системы для задач финансового риск-скоринга и прогнозирования редких с
